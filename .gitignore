# Go build output
/bin/
/gen/

# Local-only Claude Code context and tool config - never published.
/CLAUDE.md
/.claude/

# Build system output (kernel, pkgs, rootfs, images) - regenerated by the
# Makefile / self-hosted image-build workflow, never committed.
/build/
/dist/
*.img
*.iso
*.qcow2
*.ova
*.vmdk

# buf/protoc-gen-go local tool cache, if built into the repo instead of GOBIN.
/.tmp/
