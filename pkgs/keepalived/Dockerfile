# syntax=docker/dockerfile:1
#
# Optional VRRP module (keepalived) - Phase 5, NetworkService.VRRP*. Only
# built into the rootfs when enabled in a node's declarative configuration.
# No version pinned yet in ../../versions.mk. Placeholder only.

FROM scratch
