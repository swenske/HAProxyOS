# syntax=docker/dockerfile:1
#
# TODO(Phase 1): fetch+build a minimal static BusyBox applet set (only what
# the immutable rootfs's init actually needs - no interactive shell applet
# enabled, per the "no shell" design goal in ../../docs/architecture.md).
# No version pinned yet in ../../versions.mk. Placeholder only.

FROM scratch
