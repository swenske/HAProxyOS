# syntax=docker/dockerfile:1
#
# Optional firewall module (nftables) - Phase 5, NetworkService.Firewall*.
# Only built into the rootfs when enabled in a node's declarative
# configuration. No version pinned yet in ../../versions.mk. Placeholder
# only.

FROM scratch
