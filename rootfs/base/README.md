# rootfs/base

TODO(Phase 1): minimal FHS-ish directory skeleton and default `/etc`
content for the immutable rootfs (no package manager, no shell utilities
beyond what `pkgs/busybox` exposes). Empty placeholder for now - see
`../../docs/architecture.md`.
