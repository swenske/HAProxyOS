# image/disk

TODO(Phase 3): GPT A/B partition layout (BOOT-A/BOOT-B, STATE, EPHEMERAL,
META), dm-verity root hash generation, Unified Kernel Image assembly, and
Secure Boot signing (`sbsign` with the project's own key). See
`../../docs/architecture.md`.
